# Run python unit tests

name: tests

on:
  push:
  schedule:
    - cron:  "0 0 * * 1"
      branches: [ $default-branch ]

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:

    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build dev docker image
      run: docker-compose -f docker-compose-test.yml run test pytest
